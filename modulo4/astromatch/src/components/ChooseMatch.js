import react from 'react';
import styled from 'styled-components';

const ChooseMatch = (props) => {
    const [matchChosen, setMatchChosen] = useState(false)
        const onClickMatch = () => {
            if (matchChosen) {
                setMatchChosen(!matchChosen)
            } else {
                setMatchChosen(matchChosen)
            }
        }

    return(
        <div>
            <
        </div>

    )
}